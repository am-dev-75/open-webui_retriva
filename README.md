# Open WebUI for Retriva

In `docker-compose.yml`

* Change `OPENAI_API_BASE_URL` depending on where the Retriva instance runs.
* Set the port (3001 is the default value).
* Build and start the container:

```
docker compose up
```
